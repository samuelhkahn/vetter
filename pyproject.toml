[project]
name = "vetter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Kahn", email = "samuelkahn@berkeley.edu" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
vetter = "vetter:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
